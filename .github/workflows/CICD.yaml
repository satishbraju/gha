name: CICD
on:
  push
jobs:
  Build:
    runs-on: ubuntu-latest
  
    steps:
    
    - name: checkout repo
      uses: actions/checkout@v3
      
    - name: Run CI
      uses: satishbraju/mono/blob/main/.github/workflows/exp-c-CICD.yaml@main
      with:
        secrets: inherit
